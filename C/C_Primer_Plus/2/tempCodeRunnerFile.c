#include <stdio.h>
int main(void)
{
    int toes = 10;
    int two = toes * 2;
    int sq = toes * toes;
    printf("The toes = %d, the two toes = %d, and the squared of toes = %d.\n", toes, two, sq);

    return 0;
}